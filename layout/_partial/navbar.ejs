<%
var avatar = config.avatar || theme.avatar || 'https://octodex.github.com/images/baracktocat.jpg'
%>
<nav class="navbar-wrapper">
  <div class="navbar">
    <div class="container clearfix">
      <a href="<%= config.root %>" class="navbar-logo"><img src="<%= config.logo%>" title="Home" alt="Home" /></a>

      <ul class="navbar-nav float-left">
        <li class="desktop-only"><a href="<%= config.root %>">Home</a></li>
        <% if(theme.menu.archives){ %>
        <li><a href="<%= url_for(theme.archive_dir) %>">Archives</a></li>
        <% } %>
        <% if(theme.menu.categories){ %>
        <li><a href="<%= url_for(theme.category_dir) %>">Categories</a></li>
        <% } %>
        <% if(theme.menu.tags){ %>
        <li><a href="<%= url_for(theme.tag_dir) %>">Tags</a></li>
        <% } %>
        <% if(theme.menu.about){ %>
        <li><a href="<%= url_for(theme.about_dir) %>">About</a></li>
        <% } %>
        <% if(theme.rss_dir){ %>
        <li class="desktop-only"><a href="<%= url_for(theme.rss_dir) %>" target="_blank">RSS</a></li>
        <% } %>
      </ul>

      <div class="navbar-search float-right desktop-only">
        <div class="navbar-search-form">
          <label for="gsc-i-id1">This website</label>
          <div id="google-search">
            <gcse:search></gcse:search>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
